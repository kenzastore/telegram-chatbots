
services:
  bot:
    build: .
    container_name: telegram-chatbot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/finance.db
